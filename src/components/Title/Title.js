import React from 'react'

function Title({ text }) {
  return (
    <div className='
        w-full h-20
        flex items-center justify-center
        text-5xl'
      >
      {text}
    </div>
  )
}

export default Title