import React from 'react'

function Title({ text }) {
  return (
    <div className='
        w-full h-20
        flex items-center justify-center
        bg-black
        text-5xl text-gray-200'
      >
      {text}
    </div>
  )
}

export default Title